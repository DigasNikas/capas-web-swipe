# RAG

Not built yet. Placeholder chapter for retrieval-augmented question answering over the archive, once it exists.

## Status

Planned. [Image Embeddings](#image-embeddings) is the retrieval piece this would likely sit on top of: a query embedded with the same CLIP model, matched against `capas-cover-embeddings`, with the matched covers' metadata (and eventually D1 rows) fed to a model as context instead of the model guessing from parametric memory alone.

Nothing here yet. Fill in once there's a working pipeline to document.
